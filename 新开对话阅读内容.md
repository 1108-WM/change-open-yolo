# 新开对话快速恢复说明

更新日期：2026-09-07

## 2026-09-07 新分支接续入口

当前唯一开发分支为 `fi1-d-v3-geometry-semantic-separation-20260907`，S2 实现提交为 `5d423b346336a5439feb672a0225580738097b96`。S0/S1 已恢复完整多语义假设层，S2 已在不读取 val312 GT、不运行 AP 的前提下逐值复现历史 `25.415455 AP` 控制输入。本机应以该分支实际最新远程 HEAD 为准。

本机接续前必须完整阅读：

```text
docs/FI1_D_V3_GEOMETRY_SEMANTIC_SEPARATION_PREREGISTRATION_20260907.md
docs/FI1_GEOMETRY_SEMANTIC_SEPARATION_S2_B0_CONTROL_PREREGISTRATION_20260907.md
docs/FI1_GEOMETRY_SEMANTIC_SEPARATION_LOCAL_OFFICIAL100_HANDOFF_20260907.md
```

S3 尚未实现。不要在本机直接把 val312 的 S2 默认计数或 6 条 invalid-track 身份套到 official100；不要立即重跑旧 Z3、Z6 或 Qwen。先核验 official100 资产、复现 B0，并前置冻结新的迁移稳健性合同。

## 必读顺序

1. `CURRENT_EXPERIMENT_STATUS.md`：先看第一创新点冠军、全部关键 AP、safety60 最终结论和失败结论。
2. `资料/当前基线修改方向.md`：看当前唯一流程、冻结合同和第二创新点 Z0–Z6f。
3. `资料/论文阅读记录.md`：需要设计语义分布、融合或类别相关竞争时再读。

不要寻找已删除的旧文件：`当前流程与问题定位.md`、`新论文建议.txt`、三个 official 专题文件、`111 (2).txt` 和 `技术问题与答复.md`。其有效内容已合并。

## 历史 2026-08-12 状态（由上方 2026-09-07 入口覆盖）

第一创新点 official100 最终冠军为**轨迹伤害抑制 + pair-union 关系几何补全**，指标 `67.683517/83.235422/87.959075`；prior-corrected safety60 唯一有效复验为 `48.424603/65.260285/75.990715`，因 AP25 为负不跑 even48、不再调第一点。第二创新点 Z6f official100 冠军为 `32.808791/40.365582/43.812223`。其完全冻结的 safety60 单向迁移也已完成：control `32.640954/43.604318/52.040861`，Z6f `32.635704/43.590443/52.023991`（AP/AP50/AP25），delta `-0.005250/-0.013874/-0.016870` 个百分点。该结果只记录，不删 3 条 Qwen 动作、不回调 prompt/阈值/预算。第二创新点迁移验证到此结束，even48/test60 继续冻结。

## 新对话的当前任务

Z6f 分支已经封闭：不要重跑 DINO、selector/gate、Qwen、official100 AP 或 safety60 AP，也不要继续修改 prompt、A/B 对称规则、阈值、router、预算或逐动作集合。第二创新点不是理论上没有改进空间，但不能继续局部修补 Z6f；若未来决定强化它，只能在 official100 上预注册结构性新分支，例如顺序不变的多视图候选类别验证器、YOLO/Alpha/DINO 的 pairwise class verification 或显式风险/abstain 模型，且不得用 safety60 调参。

用户说“继续”时，首先分析两个创新点如何形成论文叙事、最终系统应组合哪些模块，以及还缺哪些必要的 official100 消融。当前不要立即发起新推理实验。Z6f 数值证据较弱，可先定位为语义校准扩展；只有重设核心验证器并取得稳定证据，才适合作为强独立创新点。

## 第一创新点已经冻结的内容

名称：**风险感知的多视图轨迹候选安全接入与关系几何补全**。

```text
native 候选保持不变
  + 多视图轨迹候选
  -> exact native geometry 节点折叠
  -> 轨迹质量 q 与 P(keep) 的连续伤害抑制
  -> pair-union threshold-cross 补全候选 append-only
```

最终模型包：

```text
output/train_candidate_champion_pair_union_combined_full_official100_v2_prior_corrected/model_package.pkl
output/train_candidate_champion_pair_union_combined_full_official100_v2_prior_corrected/metadata.json
```

有效 pair-union 计划是：

```text
output/train_candidate_pair_union_oof_plan_official100_v2/
```

不要恢复已删除的 v1 计划，不继续扫描 focal、指数、阈值、损失或交叉模块权重。

保留状态：F2、exact geometry 与冻结共存是稳定底座；轨迹伤害抑制保留但带 AP25 权衡标记；prior-corrected pair-union 相对伤害抑制在 official100 和 safety60 都是三项正增益，应保留。完整组合暂称 official100 冠军候选，不因 safety60 的单项负值立即删除。

## 第二创新点已完成基础与当前控制

第二创新点当前建议名称：**几何节点上的多视图开放词汇证据校准与类别条件竞争**。

核心不是重新训练 Open-YOLO 3D 主干或直接训练新的 200 类分类器，而是对冻结 geometry node 的每个 `(node, class)` 假设建立统一、跨 native/track/pair-union 可比较的真阳性排序分数。Alpha-CLIP 和 YOLO-World 都是证据源；最终重点是语义—几何联合校准与同类重复竞争。

Z0 已完成，有效结果：

```text
docs/diagnostics/z0_open_vocab_oracle_official100_20260811_v2_fixed/summary.json
```

旧目录 `docs/diagnostics/z0_open_vocab_oracle_official100_20260811/` 为 `invalid_contract`，不得引用。错误已修复于 `tools/diagnose_z0_open_vocab_oracle_gt.py`：signed cache 正确解包、类别编号空间统一、全空轨迹强制失败并输出覆盖率。有效重跑覆盖 `5,233/5,266` 条轨迹。

Z1 已完成，有效结果：

```text
docs/diagnostics/z1_yoloworld_multiview_distribution_official100_20260811_v3_frozen_support_vote/summary.json
docs/diagnostics/z1_yoloworld_multiview_distribution_official100_20260811_v3_frozen_support_vote/audit_summary.json
```

无后缀 Z1 目录和 `v2_global_keys` 均为 `invalid_contract` 中间账本，不得使用。有效 v3 的 join key 是全局唯一 `scene:id`；冻结轨迹类别来自全部 support 帧，sampled support 和 independent 分布只作可靠性证据。当前配置实际为 `198` 个有效实例 prompt。

Z2 已完成：

- 对象中心账本：`docs/diagnostics/z2_alphaclip_track_object_center_official100_20260811/`；
- 有限上下文账本：`docs/diagnostics/z2_alphaclip_track_limited_context_official100_20260811/`，固定 `crop_padding_ratio=0.50`；
- 两路均为 `5,266` 条轨迹、`5,168` 条有效语义，均通过无 GT 审计；对比见 `docs/diagnostics/z2_alphaclip_vs_z1_track_distribution_official100_20260811/summary.json`。

Z2b geometry Alpha 补全已完成：

- `docs/diagnostics/z2b_native_union_alphaclip_limited_context_official100_20260811/`：native `2,941`、pair-union `1,501`，总 `4,442`；审计 `valid=true`、`error_count=0`；native 有效 `2,934`、pair-union 有效 `1,501`。
- `docs/diagnostics/z2c_unified_semantic_node_ledger_official100_20260811/`：统一 `9,708` 节点，保存 geometry-own/inherited 的 YOLO 与 Alpha 分布，仍不读取 GT。

Z3 也已完成：

- YOLO-only：`docs/diagnostics/z3_yoloworld_control_group_official100_20260811_v1/summary.json`，frozen pair-union 精确复现 Z0，其他固定 top-1/abstain/概率乘权未提升；
- Alpha 融合：`docs/diagnostics/z3_alphaclip_fusion_control_group_official100_20260811_v1/summary.json`；最佳为 `frozen_context_equal_top1`，pair-union `30.311670/36.918962/39.993122`，相对 Z0 `+0.792701/+1.289969/+1.619441`（AP/AP50/AP25）。

Z3 official100 OOF 校准已完成：

- 监督集：`docs/diagnostics/z3_semantic_reliability_dataset_official100_20260811/`，`66,589` 条有效 `(node,class)` 假设；只用 official train GT 生成标签。
- 五折 OOF：`docs/diagnostics/z3_semantic_reliability_oof_official100_20260811/`，固定 manifest SHA-256 `aa657449...3e3e`，低容量 `HistGradientBoostingRegressor`，不含类别 ID。
- AP：`docs/diagnostics/z3_semantic_reliability_oof_ap_official100_20260811_v2_union_frozen_score_control/`。固定 Alpha 控制精确复现；native/track joint OOF、union 冻结低分的 hybrid 为 `32.755740/40.298027/43.769243`，五折 AP delta `+3.1695/+1.0575/+4.1597/+3.3377/-3.2307`。直接对 union 使用 OOF 分数只有 `31.019096`；由此启动的同类竞争和稳定性阶段已在 Z4–Z4d 完成。

Z4 首轮已完成但未冻结：

- 同类 IoU≥0.50 winner 基本中性；fixed top-5 为 `32.858441/40.383017/43.907369`，但五折仅 3/5 正且 head AP 下降。
- adaptive top-5 + 95% score mass、cap20 为 `32.789395/40.337377/43.812404`，仍只有 3/5 正。
- one-to-one Hungarian 竞争监督把 direct union 恢复到 `32.697318 AP`，证明标签方向有效，但绝对值低于 hybrid；只对 union 使用该模型的混合为 `32.745047 AP`，也未提升。
- 详细目录见 `CURRENT_EXPERIMENT_STATUS.md`。当前禁止继续扫描 top-M、IoU、类别平衡强度、模型容量和融合权重。

Z4b–Z4d 结论：正式 Z4b 审计证明 fold 4 主要受极稀疏类别的宏 AP 方差影响；Z4c 排除 union 训练样本后更差；Z4d 完全类别等权使五折主 AP 全正并把 fold 4 修到 `+0.3120 AP`，但全量只有 `32.123798/39.577321/43.722213` 且 head AP 回退。因此该阶段控制为 `32.755740/40.298027/43.769243`，Z4 不再继续调权；后续 Z5-Z6f 及 safety60 迁移均已完成。

历史执行顺序（现已全部完成或终止）：

```text
Z1  完整 YOLO-World 多视图 198 类分布账本（已完成）
Z2  Alpha-CLIP 对象中心／上下文独立语义账本（已完成）
Z3  训练自由融合控制组 + official100 OOF 类别无关可靠性校准（已完成）
Z4  exact-geometry 类别聚合与组件内类别相关竞争（首轮已完成，未冻结）
Z4b frozen fold 4 分布漂移与可靠性稳定性审计（已完成）
Z4c 训练—应用来源合同对齐（已完成并撤销：全量与 fold 4 均更差）
Z4d 来源内 predicted-class 等总训练权重（已完成：五折全正、fold 4 修复，但全量和 head 较差，不冻结）
Z5 语义辅助 merge/split/boundary-owner oracle 上界（Z5a/Z5b 已完成；Z5c 因上界不足终止）
Z6 类别候选内多视图视觉复核（Z6f 已完成；safety60 单向迁移已结束）
```

正式 Z4b 目录是 `docs/diagnostics/z4b_fold_distribution_shift_official100_20260812_v3_hybrid_rank_audit/`；更早的无后缀和 v2 目录是中间输出，不得引用。Z4c/Z4d AP 目录分别为 `docs/diagnostics/z4c_semantic_reliability_oof_ap_official100_20260812_native_track_fit/` 和 `docs/diagnostics/z4d_semantic_reliability_oof_ap_official100_20260812_class_balanced/`。

## Z5a/Z5b 已完成；Z5c 终止

Z5a 正式目录：

```text
docs/diagnostics/z5a_semantic_geometry_action_space_official100_20260812/
```

有效结论：Z5a merge `1,501` 个、100 场景全覆盖、三方语义 join `100%`、候选修改数 0；split 与 boundary-owner 因缺少 official100 已物化可追溯动作空间而终止。Z5b 只选出 `17` 个 GT-only 动作，覆盖 `14` 场景、`12` 类别，主 AP 五折虽 `5/5` 微正，但 oracle 相对 no-op 为 `+0.018038/-0.000899/-0.001556`，相对当前全部 union 控制主 AP仅 `+0.002614`。TP25 无新增 crossing、TP50 仅 1。上界不足，Z5c 不启动；GT 选择不得写回推理计划。

Z5b 正式目录：

```text
docs/diagnostics/z5b_merge_global_feasible_oracle_official100_20260812/
```

## Z6 已完成；safety60 单向迁移已结束

Z6a 正式目录：

```text
docs/diagnostics/z6a_class_candidate_space_oracle_official100_20260812/
```

当前分数下，frozen/YOLO top-5/Alpha top-5/YOLO+Alpha union/full-class 的 AP 分别为 `32.755740/36.007068/41.880954/43.285077/54.734264`；一对一分数下分别为 `51.771848/53.800346/58.558014/59.953029/69.837770`。TP25/TP50 的 union 正确类别覆盖为 `92.7348%/93.3477%`，节点 target 覆盖为 `89.9419%/90.4013%`，可修复当前错误比例为 `92.3879%/93.0712%`。Alpha 与 union 的 current-score AP delta 都是五折 `5/5` 正；当前控制复现最大误差 `0.0`。

有效结论：正确类别通常已在现有 YOLO/Alpha 候选集合内。不要引入第三类别生成器；不要把 DINOv2 当 198 类分类器；不要全量调用 MLLM。以下 Z6b-Z6f 段落是已完成阶段的历史摘要，其中当时的执行顺序已被最终 safety60 结论覆盖。

Z6b 第一阶段已经完成：

```text
docs/diagnostics/z6b_object_view_manifest_official100_20260812/
```

正式结果为 `100/100` 场景、`9,708/9,708` 节点 join；`9,603` 个节点有固定视角，`105` 个节点因冻结 `min_visible_points=20` 合同无视角；共 `26,483` 个视角，所有 RGB/depth/pose/intrinsics 资产存在。节点 0/1/2/3 视角数为 `105/287/1,752/7,564`。manifest SHA-256 为 `503261f316a0e9e642eb09c63c87d1fd10c9be60b3bd71041d3dfa8fb149107b`。当时的无 CUDA 退出只是历史记录；后续 GPU 已获授权并完成正式 DINO 账本。

GPU 已获授权，Z6b DINO 正式账本现已完成并独立审计 `error_count=0`：`26,483` 视角、`9,603` 有 embedding 节点，跨视角 cosine mean `0.836047`，dispersion p90 `0.307926`。Z6c review input 和 selector supervision 也已完成。direct 五折 selector 会改类约 `89.2%`，AP 反而降到 semantic-only `28.236506`、semantic+DINO `28.124939`，故 direct argmax 已终止，不能引用为改进；其后的 nested gate 也已完成。

该 nested gate 及唯一一次目标修正也已完成。连续 delta gate 因 wrong-to-wrong 被当作零增量而接受约 `88.5%` 提议，AP 仅 semantic-only `28.418329`、semantic+DINO `29.205986`，均五折 `0/5` 正。随后固定二元 improvement gate（`P(true improvement)>0.5`，无阈值扫描）把接受率降到约 `23%`，但 AP 仍为 semantic-only `31.889356/39.328924/42.816283`（`2/5` 正）、semantic+DINO `31.496255/38.893321/42.354466`（`1/5` 正），低于 `32.755740/40.298027/43.769243`。自动 selector/gate 分支正式终止；随后有限预算视觉复核已作为 Z6e/Z6f 完成。

Z6e/Z6f 视觉复核随后已完成并产生新 official100 冠军。固定路由只复核 Z6d semantic-only 接受、YOLO/Alpha top1 冲突、3 个冻结视角、DINO cosine mean `>=0.855297`、每节点最多 1 个，共 `82` 项。下载并固定 `Qwen2.5-VL-7B-Instruct@cc594898...`；三 crop 分辨率合同为 `100352..200704 pixels`。单顺序版本 0 改类；唯一一次对称 A/B 次序复核得到 `74 ABSTAIN + 8 PROPOSED`，8 项在 GT 前冻结。official100 达到 `32.808791/40.365582/43.812223`，相对旧冠军 `+0.053051/+0.067555/+0.042980`；五折 1 正、1 个 `-0.002287 AP` 微负、3 no-op。正式汇总见 `docs/diagnostics/z6f_vlm_selector_ap_summary_official100_20260812/`。不得根据逐动作 GT 删选，也不得再调 prompt、分辨率、DINO 阈值、预算或路由。其唯一一次 safety60 单向迁移已完成，结果见下一段。

该 safety60 单向迁移现已完成。输入 adapter 保留 native `36,000`、track `3,569`、pair-union `1,520`；统一账本 `7,764` 节点，DINO `19,156` 视角且独立审计无错误。冻结 full bundle 的 `7,655` 个 accepted proposal 经固定 router 后只送审 `61` 项；Qwen 输出 `56 ABSTAIN + 2 CURRENT + 3 PROPOSED`。三项动作是 `counter→kitchen counter`、`projector screen→whiteboard`、`folded chair→chair`。最终 control/Z6f 为 `32.640954/43.604318/52.040861` 与 `32.635704/43.590443/52.023991`，三项主指标均轻微下降。正式结果：`docs/diagnostics/z6f_safety60_transfer_ap_20260812/`。不得基于该结果删动作或回调方法；even48/test60 继续冻结。

以下为已完成的 Z5a 历史合同，保留供审计，不是新对话待执行任务：

Z5 的目的不是继续校准分类分数，而是回答：在冻结的 native/track/pair-union 几何之外，完整 YOLO-World + Alpha-CLIP 软分布能否可靠指导 merge、split 或 boundary-owner，并带来独立的开放词汇实例 AP 上界。

当时 Z5a 按以下合同执行：**只读、无 GT、无 AP、无候选修改**。

1. 检查 official100 当前已有的冻结几何和语义产物，建立来源与 join-key 合同：
   - 场景清单：`output/scannet200/scene_splits/official_train100_20260808/official_train100.txt`；
   - frozen split：`output/train_candidate_quality_oof_official100_v2/split_manifest.json`，实际 SHA-256 为 `aa657449965bc76164a1a1b77c7785aa705a0295eeed1307163b325f7233fe3e`；
   - 统一语义节点：`docs/diagnostics/z2c_unified_semantic_node_ledger_official100_20260811/`，共 `9,708` 个节点；
   - 当前开放词汇 OOF 分数：`docs/diagnostics/z3_semantic_reliability_oof_official100_20260811/`；
   - 当前 hybrid AP 控制：`docs/diagnostics/z3_semantic_reliability_oof_ap_official100_20260811_v2_union_frozen_score_control/`；
   - 已有 append-only pair-union：`output/train_candidate_champion_pair_union_combined_oof_plan_official100_v1/`，共 `1,501` 个。
2. 分别盘点三类动作是否在 official100 上已有可复用、可追溯的冻结动作空间：
   - `merge`：优先复用已存在的 pair-union 及其两个父候选，不再扫描新的 IoU/距离阈值；
   - `split`：只有发现已经物化且能追溯到父候选的子 mask/点集时才纳入；若不存在，明确报告 unavailable，不得临时生成伪 split；
   - `boundary-owner`：只有 official100 存在可连接的 superpoint/owner 账本时才纳入。旧 safety60 MV3DIS owner 分支不能直接迁移，也不能重新调其阈值。
3. 对每个可用动作输出 no-GT 合同账本，至少包含：scene、action id、action type、父/子 geometry key、point count、重叠/包含/接触关系、来源、共同可见证据、geometry/inherited YOLO 与 Alpha 的 top probability、margin、entropy、JS/agreement、语义可用性和缺失原因。
4. 输出 join/coverage 审计：动作总数、100 场景覆盖、各来源数量、语义节点 join 成功率、缺失/重复/非法 geometry 数、是否读取 GT、是否修改候选。建议正式目录：
   `docs/diagnostics/z5a_semantic_geometry_action_space_official100_20260812/`。
5. Z5a 完成前不得设计动作分类器、阈值或 AP 修正规则，也不得运行 safety60/even48/test60。

Z5a 通过后才进入 **Z5b GT-only oracle**：在 official train100 上对每个冻结动作比较 `no-op` 与动作反事实，先产出逐动作 IoU/TP25/TP50/类别正确性变化和场景/类别归因，再聚合 global-feasible oracle AP；必须同时报告 AP/AP50/AP25、head/common/tail 和 frozen 五折。GT 只用于显式 oracle/评测，oracle 选择不得写回推理计划。只有上界在多折和多场景中稳定存在，才允许进入 Z5c no-GT plan-only；否则终止对应动作族。

特别注意：旧结论中“不要继续 boundary-owner”是指不要恢复已失败的 safety60/MV3DIS 参数分支；Z5 只允许审计 official100 上是否存在新的、由完整语义软分布约束且无需重新扫描几何阈值的动作上界。

### 第二点训练与评测合同

- Z0、Z1、Z2 先做无训练账本与 oracle/控制组；不训练 Open-YOLO 3D、Mask3D 或新的三维主干。
- official train100 的低容量、类别无关校准器及 Z4 稳定性审计已经完成；当前不再训练或扫描新的校准变体。已有模型预测 `(geometry node, class)` 真阳性质量，不直接学习 200 类类别替换。
- official100 必须使用场景隔离五折 OOF：每折仅用 4/5 场景拟合，剩余 1/5 只做 OOF；策略冻结后才可用 official100 全量拟合最终校准器。
- safety60 不能用于训练、选阈值、选融合权重或回调；只允许加载冻结模型/规则后做一次最终迁移 AP。
- even48 只能冻结后原样重放，test60 继续冻结。GT 只能进入 official train 监督、显式 oracle 和最终 evaluator，不得进入无 GT 推理账本。

## safety60 已完成结论

最终组合已按自然正例率 `0.047577720588447926` 修复 pair-union 概率合同并完成唯一一次冻结 AP。corrected v2 结果为 `48.424603/65.260285/75.990715`，相对冻结共存 `+0.234963/+0.209909/-0.177901`；候选文件修改数为 0，pair-union 仅追加，AP 聚合次数为 1。AP25 为负，因此不运行 even48，不据此调整阈值、损失、指数或权重。

旧 v1 的 `48.353545/65.138110/75.825344` 直接使用 50/50 平衡拟合的原始概率，遗漏 prior correction，属于 `invalid_contract`，只保留审计，不能作为实验结论。有效结果目录是 `docs/diagnostics/safety60_champion_pair_union_combined_class_agnostic_ap_20260811_v2_prior_corrected/`。

## 当前开放词汇问题

safety60 纯 native 为 `29.971029/38.686177/44.231502`，native+F2 为 `29.820266/38.247102/43.621551`。类别无关几何提升没有转成开放词汇 AP，原因集中在完整类别分布缺失、native/track 分数不可比和类别相关重复竞争。新会话不要再从增加几何候选数量开始。

## 数据与操作纪律

- official train：允许场景隔离训练和五折 OOF。
- safety60：回顾性开发／迁移复验；不训练、不选阈值、不回调。
- even48：已使用的 robustness set，只允许冻结原样重放。
- test60：尚未运行并继续冻结；不要把 official100 五折 OOF 或 safety60 写成 test60 结果。
- GT：只用于 official train 监督、显式 oracle 和评测。
- 当前 Git 工作区很脏，用户决定暂不清理；不要运行 `git clean`、`git reset` 或回退用户修改。
- 2026-08-11 已清理的路径见 `docs/cleanup_manifest_20260811.md`；不要删除其余正式大缓存或当前冠军产物。

## 继续工作前的最小检查

1. 阅读上述三个状态文件，不依赖旧对话记忆。
2. 确认最终冠军模型包和 official100 summary 仍存在。
3. 不重复运行已经完成的 official100 Z0–Z6、DINO、Qwen、AP 或 safety60 单向迁移；Z5c 和 Z6f 均已封闭。
4. safety60 不能用于训练、选阈值、选融合权重或回调；第二创新点唯一一次迁移已经结束。
5. 若用户说“继续”，不得重复运行已完成的 safety60、official100 Z0–Z6 或回放 even48/test60，也不得删除第一点冻结模块；下一步应先分析两个创新点的论文叙事、最终系统组合与尚缺的必要消融，不再从 safety60 调参。
