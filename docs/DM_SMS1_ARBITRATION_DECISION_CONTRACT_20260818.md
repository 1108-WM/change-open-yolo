# DM-SMS-1 类别仲裁决定合同

对每个 geometry 独立读取两种候选顺序的证据：

1. 候选正序；
2. 候选完全反序。

只有同一个替代类别在两种顺序中都被标记为有支持证据，并且两种顺序都没有强反证时，才允许把冻结原类别替换为该类别。否则保持冻结原类别。

决定同时应用到所有 geometry。每个 geometry 最多替换一次类别。决定过程不允许删除候选、不允许改变 geometry、掩码、成员关系或排序分数。

当前代码只实现校验和决定函数，没有实际模型证据输入，因此尚未生成类别修改结果，也没有运行 AP。
